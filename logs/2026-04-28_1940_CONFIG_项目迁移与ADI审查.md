# [CONFIG] 项目迁移与 ADI 全量审查

- **时间**：2026-04-28 19:40 (UTC+8)
- **项目**：switcher-v5（API 配置切换器 / C-code-switch）
- **关联 Session**：8d5ba99d-254f-417b-927f-5e5abd294d73
- **影响范围**：整个项目（目录位置变更 + 全量代码审查）

---

## 📝 做了什么（What）

### 1. 项目目录迁移
- 将项目从 `/Users/zz/Downloads/switcher-v5` 移动至 `/Users/zz/switcher-v5`
- 使用 `mv` 命令完成，`.git` 历史完整保留
- Grep 扫描确认：**项目内无硬编码旧路径引用**，迁移安全

### 2. ADI 全量代码审查

逐文件审查了以下所有源文件：

| 文件 | 行数 | 状态 | 备注 |
|------|------|------|------|
| `package.json` | 39 | ✅ OK | v6.0.0, Electron 29, 配置完整 |
| `main.js` | 55 | ✅ OK | Electron 主进程，`__dirname` 相对路径，迁移无影响 |
| `server.js` | 163 | ✅ OK | HTTP 服务，`os.homedir()` + `__dirname` 动态路径 |
| `index.html` | 927 | ✅ OK | 前端 UI + 全部 JS 逻辑，API 地址 `localhost:7823` |
| `启动.command` | 39 | ✅ OK | `cd "$(dirname "$0")"` 自适应路径 |
| `.gitignore` | 6 | ✅ OK | 排除 node_modules/dist/log/scpt |
| `README.md` | 95 | ⚠️ 小问题 | 见下方 |
| `使用说明.md` | 46 | ⚠️ 小问题 | 见下方 |

---

## 🔍 ADI 审查发现

### ✅ 无问题项

1. **路径安全**：所有代码均使用 `__dirname`、`os.homedir()`、`$(dirname "$0")` 等动态路径，无硬编码绝对路径，迁移无风险
2. **双平台隔离**：`server.js` 的 `BLOCKS` 对象将 Claude 和 Codex 的环境变量块完全隔离，互不干扰
3. **zshrc 块标记**：使用 `# Claude Code Configuration (Proxy)` / `# /Claude Code Configuration` 精确匹配 + 替换，不会误删其他配置
4. **Shell 注入防护**：`escapeShellValue()` 对 `\\"$\`!` 做了转义处理
5. **XSS 防护**：前端 `esc()` 函数对 HTML 特殊字符做了转义
6. **CORS 控制**：`server.js` 仅允许 `localhost:7823` 和 `127.0.0.1:7823` 来源
7. **历史快照**：每次同步前自动保存快照，最多 10 条，防误操作
8. **旧版数据迁移**：`hydrate()` 自动从 `claude_cfgs_v4` 迁移到 `api_cfgs_v6`

### ⚠️ 非阻塞问题（建议后续优化）

| # | 问题 | 文件 | 严重度 | 说明 |
|---|------|------|--------|------|
| 1 | 文档版本不一致 | `使用说明.md` | P3 | 标题写"v4"，但实际已是 v6 |
| 2 | package.json `name` 字段 | `package.json` | P3 | `claude-code-switcher` 未体现已支持 Codex 双平台 |
| 3 | `.scpt` 调试文件残留 | `a.scpt`, `test.scpt` | P3 | 虽已 gitignore 排除，但本地有两个调试用 AppleScript 文件 |
| 4 | 端口冲突无提示 | `server.js` | P3 | 若 7823 被占用，`server.listen` 会报错但无友好提示 |
| 5 | `setTimeout 1000ms` 启动等待 | `main.js:24` | P3 | Electron 内用固定延迟等待 server 启动，不如用 health check 轮询稳健 |

### ✅ 安全性审查

- **无敏感信息泄露**：API Key 仅存在用户本地 localStorage 和 `~/.zshrc`，不上传到任何远程
- **无远程请求**：server.js 仅本地 HTTP，无外部网络调用
- **无 eval / IIFE 风险**：前端 JS 无危险的动态代码执行

---

## 💡 关键技术决策（How / Decision）

- **迁移策略**：使用 `mv` 而非 `cp + rm`，保持 `.git` 目录完整性，避免 inode 变化导致的权限问题
- **审查范围**：本次为 CONFIG 类操作，虽然没有代码变更，但执行了完整的 ADI 风格审查，确保迁移后项目完整性

---

## ⚠️ 经验总结 / 避坑指南（Lessons Learned）

- **最佳实践**：项目中所有路径引用均使用 Node.js / Shell 的动态路径 API（`__dirname`、`os.homedir()`），使得目录移动零风险。这是正确的做法，值得在其他项目中推广
- **注意**：`使用说明.md` 的版本标题长期未同步更新（写着 v4），说明文档更新容易被遗忘，建议每次版本升级时加入文档同步 checklist

---

## 🔗 关联资源

- GitHub 仓库：[C-code-switch](https://github.com/zxs-ai/C-code-switch)
- 关联历史 Session：33d37a41（同步按键 AppleScript 调试）、31f4a757（双平台 UI 开发）
